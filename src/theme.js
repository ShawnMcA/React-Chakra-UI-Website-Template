import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  fonts: {
    heading: "'Nunito', sans-serif", 
    body: "'Nunito', sans-serif",
    stylized: "'Oleo Script Swash Caps', cursive"
  }
});

export default theme;